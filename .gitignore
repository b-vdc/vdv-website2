desktop.ini
Thumbs.db
.DS_Store

# Contact form secrets (see config.example.php) — never committed, created on the server
config.php

# Runtime storage (rate-limit bookkeeping, Google token cache, booking locks).
# The .htaccess stays committed so none of it is ever web-readable.
data/*
!data/.htaccess
